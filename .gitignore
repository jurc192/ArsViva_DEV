# Do not include GULP stuff
node_modules/
gulpfile.js
package.json

#Don't include server settings
.htaccess

# Don't add the settings (passwords) file to the repository #
wp-config.php

# If using SASS to compile stylesheets this saves conflicts, plus there not needed! #
wp-content/themes/YOURTHEME/.sass-cache/

# No need to add the upgrade folder to the repo #
wp-content/upgrade/

# Don't include uploads folder
wp-content/uploads/

# sitemaps not needed locally #
sitemap.xml
sitemap.xml.gz
