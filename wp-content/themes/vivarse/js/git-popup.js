/* My function to show a pop up message, saying on which GIT branch is server atm */
function gitPopup() {
  var gitText = document.getElementById("gitText");
  var gitContainer = document.getElementById("gitContainer");

  gitContainer.style.visibility = "hidden";
}
